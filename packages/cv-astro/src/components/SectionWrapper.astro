---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="flex min-h-0 flex-col gap-y-3">
  <h2 class="text-xl font-bold">{title}</h2>
  <section class="flex min-h-0 flex-col gap-y-8">
    <slot />
  </section>
</section>